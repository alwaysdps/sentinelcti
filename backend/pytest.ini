[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers

# Note: newer Starlette releases emit a one-off StarletteDeprecationWarning at
# import time suggesting httpx2 for TestClient. It fires before pytest's
# filters apply, so it is left visible rather than papered over with a filter
# that does not actually suppress it.
